import { ModuleItemDirective } from './module-item.directive';

describe('ModuleItemDirective', () => {
  it('should create an instance', () => {
    const directive = new ModuleItemDirective();
    expect(directive).toBeTruthy();
  });
});
